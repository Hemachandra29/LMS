const express = require('express');
const router = express.Router();

// TODO: Implement course routes
// - GET /api/courses (public - list published courses)
// - GET /api/courses/:id (public - get course details)
// - POST /api/courses (instructor/admin - create course)
// - PUT /api/courses/:id (instructor/admin - update course)
// - DELETE /api/courses/:id (instructor/admin - delete course)
// - GET /api/courses/instructor/:id (instructor - get instructor's courses)
// - POST /api/courses/:id/publish (instructor/admin - publish course)
// - POST /api/courses/:id/unpublish (instructor/admin - unpublish course)

module.exports = router; 