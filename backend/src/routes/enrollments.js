const express = require('express');
const router = express.Router();

// TODO: Implement enrollment routes
// - POST /api/enrollments (student - enroll in course)
// - GET /api/enrollments/student/:id (student - get student enrollments)
// - GET /api/enrollments/course/:id (instructor/admin - get course enrollments)
// - PATCH /api/enrollments/:id/status (instructor/admin - update enrollment status)
// - DELETE /api/enrollments/:id (student - drop course)

module.exports = router; 